# 用户偏好设置

- 用户明确要求：不要生成总结性Markdown文档；不要生成测试脚本；不要编译；不要运行，用户自己编译/运行。
- 用户再次强调：不要生成总结性Markdown文档；不要生成测试脚本；不要编译；不要运行，用户自己编译/运行。
- 用户持续强调：不要生成总结性Markdown文档；不要生成测试脚本；不要编译；不要运行，由用户自己编译/运行。
- 用户明确要求：不要生成总结性 Markdown 文档；不要生成测试脚本；不要编译；不要运行，用户自己编译/运行。
